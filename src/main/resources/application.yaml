spring:
  application:
    name: ShareItem
  datasource:
    url: jdbc:postgresql://localhost:5432/si_db
    driverClassName: org.postgresql.Driver
    username: stas
    password: stas
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect